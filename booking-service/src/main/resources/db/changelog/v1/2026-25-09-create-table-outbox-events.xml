<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="005-create-outbox-events" author="bulat">
        <createTable tableName="outbox_events">
            <column name="id" type="UUID">
                <constraints
                        primaryKey="true"
                        nullable="false"
                        primaryKeyName="pk_outbox_events"/>
            </column>

            <column name="event_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="aggregate_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="aggregate_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="payload" type="TEXT">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="published_at" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>

        <createIndex
                tableName="outbox_events"
                indexName="idx_outbox_events_status_created_at">

            <column name="status"/>
            <column name="created_at"/>

        </createIndex>

        <createIndex
                tableName="outbox_events"
                indexName="idx_outbox_events_aggregate_id">

            <column name="aggregate_id"/>

        </createIndex>

    </changeSet>

</databaseChangeLog>