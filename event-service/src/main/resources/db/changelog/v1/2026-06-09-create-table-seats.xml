<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">

    <changeSet id="003-create-table-seats" author="Bulat">
        <createTable tableName="seats">

            <column name="id" type="UUID">
                <constraints
                        primaryKey="true"
                        nullable="false"
                        primaryKeyName="pk_seats"/>
            </column>

            <column name="section_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="row_number" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="seat_number" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="x" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="y" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="width" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="height" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="rotation" type="INTEGER">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <addForeignKeyConstraint
                baseTableName="seats"
                baseColumnNames="section_id"
                referencedTableName="sections"
                referencedColumnNames="id"
                constraintName="fk_seats_section"/>

        <addUniqueConstraint
                tableName="seats"
                columnNames="section_id, row_number, seat_number"
                constraintName="uk_seats_position"/>

        <createIndex
                tableName="seats"
                indexName="idx_seats_section_id">

            <column name="section_id"/>

        </createIndex>
    </changeSet>
</databaseChangeLog>