<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="005-create-table-idempotency-keys" author="Bulat">

        <createTable tableName="idempotency_keys">

            <column name="id" type="UUID">
                <constraints
                        primaryKey="true"
                        nullable="false"
                        primaryKeyName="pk_idempotency_keys"/>
            </column>

            <column name="key" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="operation" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="resource_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <addUniqueConstraint
                tableName="idempotency_keys"
                columnNames="key, operation"
                constraintName="uk_idempotency_keys_key_operation"/>

        <createIndex
                tableName="idempotency_keys"
                indexName="idx_idempotency_keys_resource_id">
            <column name="resource_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>